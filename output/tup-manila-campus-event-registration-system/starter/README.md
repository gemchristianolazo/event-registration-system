# tup-manila-campus-event-registration-system — starter scaffold

Generated by the DEVCON Jumpstart Agent Kit from your ADR's chosen stack. This is a working hello-world, not your finished product — replace it feature by feature starting from FR-01 in your PRD (or `mvp-scope.md`'s Build Now list if you ran `/scope`).

## What's here
- **Frontend:** react-vite
- **Backend:** node-express
- **Database:** supabase

## Run it
1. `cd frontend && npm install && npm run dev`
2. `cd backend && npm install && npm start` (then visit `/api/health`)

## Next steps
1. Open your PRD (or mvp-scope.md) and pick your first feature.
2. Replace the hello-world page/route with that feature.
3. Keep this README's run instructions accurate as you add real setup steps.
